<h1>User</h1>

<table>
    {{#each model.attributes}}
        <tr>
            <td>
                <strong>{{this.key}}:</strong>
            </td>
            <td>
                {{input value=''}}
            </td>
        </tr>
    {{/each}}
</table>

<button {{action 'save'}}>Create</button>

{{outlet}}
